- name: Run Postman Collection
  run: |
    newman run tests/postman/recev_account_detail.postman_collection.json \
    --reporters cli,html \
    --reporter-html-export report.html

- name: Upload HTML Report
  uses: actions/upload-artifact@v4
  with:
    name: postman-report
    path: report.html